<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huge.aiexchange.mapper.StockBaseMapper">

    <!-- 批量插入股票数据（忽略重复） -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT IGNORE INTO stock_base 
        (stockCode, time, open, high, low, close, volume)
        VALUES 
        <foreach collection="list" item="item" separator=",">
            (#{item.stockCode}, #{item.time}, #{item.open}, #{item.high}, #{item.low}, #{item.close}, #{item.volume})
        </foreach>
    </insert>

</mapper>